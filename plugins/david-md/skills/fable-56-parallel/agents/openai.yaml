interface:
  display_name: "Fable + 5.6 Parallel"
  short_description: "Show two complete answers unchanged"
  default_prompt: "Use $fable-56-parallel to ask Fable High and GPT-5.6 Sol High the same question at the same time and show both answers exactly as written."
policy:
  allow_implicit_invocation: false
