interface:
  display_name: "Fable + 5.6 Parallel"
  short_description: "Compare two verbatim high-effort answers"
  default_prompt: "Use $fable-56-parallel to send this task to Fable High and GPT-5.6 Sol High in parallel and show me both raw answers."
policy:
  allow_implicit_invocation: false
