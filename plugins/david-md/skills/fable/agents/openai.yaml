interface:
  display_name: "Fable"
  short_description: "Run Fable with explicit task limits"
  default_prompt: "Use $fable for this task with explicit limits. Use Opus 5 only after a recorded Fable model limit."

policy:
  allow_implicit_invocation: false
