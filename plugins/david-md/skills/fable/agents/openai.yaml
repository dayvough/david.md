interface:
  display_name: "Fable"
  short_description: "Run bounded Fable work with audited Opus fallback"
  default_prompt: "Use $fable for this bounded task, with the mode determined by my request and Opus 5 only for an audited Fable boundary."

policy:
  allow_implicit_invocation: false
